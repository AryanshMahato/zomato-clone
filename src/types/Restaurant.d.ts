export interface Category {
  id: number;
  name: string;
}

export interface Cuisines {
  id: number;
  name: string;
}

export interface AllRestaurant {
  id: string;
  name: string;
  timings: string;
  rating: string;
  phoneNumber: string;
  location: string;
}
